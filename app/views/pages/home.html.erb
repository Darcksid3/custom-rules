<%= render "shared/flashes_ok" %>
<h3 class="mt-4 text-center">la seule application <strong>gratuite</strong> qui vous génère des <em>funny rules</em> de vos jeux préférés !</h3>
<div class="game-card-main d-flex">
  <% @games.each do |game| %>
  <%= render "shared/card", game:game %>
  <% end %>
</div>
