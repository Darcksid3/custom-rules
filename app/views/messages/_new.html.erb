<%= simple_form_for [@conversation, @message] do |f| %>
  <%= f.input :content, required: true, label: "Copy and paste a long text here" %>
  <%= f.button :submit, 'Create tweet', class: 'btn btn-primary' %>
<% end %>
