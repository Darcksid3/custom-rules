<% if @conversation.title_previously_changed? %>
  <%= turbo_stream.update "titre", @conversation.title %>
<% end %>

<%= turbo_stream.update "new_message_container" do %>
  <%= render "messages/form", conversation: @conversation, message: Message.new %>
<% end %>